{
  "name": "freecell-plus",
  "private": true,
  "description": "Tampermonkey userscript for Solitaire Bliss FreeCell — test tooling only, the script itself has no build step.",
  "scripts": {
    "test": "node --test"
  }
}
